# secrets/ README

- `notion.env` will hold Notion OAuth credentials (client ID / secret).
- This directory is git-ignored.
- Populate the env file manually; keep permissions restrictive (chmod 600).
